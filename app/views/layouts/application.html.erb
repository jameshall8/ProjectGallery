<!DOCTYPE html>
<html>
  <head>
    <title>ProjectGallery</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <body class="d-flex flex-column h-100">
    
    <header>
      
      <!-- Fixed navbar -->
      <nav class="navbar navbar-expand-md navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">James Hall Gallery</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
              <li class="nav-item">
                <a class="nav-link" aria-current="page" <%= link_to "Home", root_path%></a>
                <% @categoryNav.each do |category| %>
                  <li class="nav-item">
                  <a class="nav-link" aria-current="page"<%= link_to category.name, category_path(category.id)%></a>
                  </li>
                <% end %>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Category</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
              </li>
            </ul>
            <ul class="navbar nav navbar-nav">
              <li><a class="nav-link" href="#">Upload photo</a></li>
              <li><a class="nav-link" href="#">Add category</a></li>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    
      <section>
      <div class="container">
      <%= yield %>

      </div>
    </section>
    
    
    
    
    
    <footer class="footer mt-auto py-3 bg-dark">
      <div class="container">
        <span class="text-muted">James Hall - Gallery</span>
    </footer>
  </body>
</html>
